# Base URL of the Django API (no trailing slash).
# Local development (optional: this is already the default when running `npm run dev`):
VITE_API_URL=http://127.0.0.1:8000
# Production (set this in Vercel -> Project Settings -> Environment Variables):
# VITE_API_URL=https://YOUR_USERNAME.pythonanywhere.com
